extends auth

block auth
  form.signup.form-horizontal(action="/users", method="post")
    .form-group
      label.control-label.col-md-4(for='name') Full name
      .col-md-6
        input#name.form-control(type='text', name="name", placeholder='Full name', value=user.name)

    .form-group
      label.control-label.col-md-4(for='email') Email
      .col-md-6
        input#email.form-control(type='text', name="email", placeholder='Email', value=user.email)

    .form-group
      label.control-label.col-md-4(for='username') Username
      .col-md-6
        input#username.form-control(type='text', name="username", placeholder='Username', value=user.username)

    .form-group
      label.control-label.col-md-4(for='password') Password
      .col-md-6
        input#password.form-control(type='password', name="password", placeholder='Password')

    .well
      button.btn.btn-primary.col-md-offset-4(type='submit') Sign up
      &nbsp;
      | or&nbsp;
      a.show-login(href="/signin") login
